docker exec -it kafka /opt/kafka/bin/kafka-consumer-groups.sh \
  --bootstrap-server 192.168.10.250:9094 \
  --command-config /tmp/client.properties \
  --list

docker exec -it kafka /opt/kafka/bin/kafka-topics.sh \
  --bootstrap-server 192.168.10.250:9094 \
  --command-config /tmp/client.properties \
  --describe --topic orders

docker exec -it kafka /opt/kafka/bin/kafka-topics.sh \
  --bootstrap-server 192.168.10.250:9094 \
  --command-config /tmp/client.properties \
  --create --topic orders --partitions 3 --replication-factor 1

docker exec -it kafka /opt/kafka/bin/kafka-topics.sh \
  --bootstrap-server 192.168.10.250:9094 \
  --command-config /tmp/client.properties \
  --alter --topic orders --partitions 6

docker exec -it kafka /opt/kafka/bin/kafka-topics.sh \
  --bootstrap-server 192.168.10.250:9094 \
  --command-config /tmp/client.properties \
  --delete --topic orders

## see all consumer group in all topics

docker exec -it kafka /opt/kafka/bin/kafka-consumer-groups.sh \
  --bootstrap-server 192.168.10.250:9094 \
  --command-config /tmp/client.properties \
  --describe --all-groups

# how to see state of consumer group (empty, stable)

docker exec -it kafka /opt/kafka/bin/kafka-consumer-groups.sh \
  --bootstrap-server 192.168.10.250:9094 \
  --command-config /tmp/client.properties \
  --describe --group fund-transfer --state

# reset offset to 100 before

docker exec kafka /opt/kafka/bin/kafka-consumer-groups.sh \
  --bootstrap-server 192.168.10.250:9094 \
  --command-config /tmp/client.properties \
  --group <GROUP-NAME> \
  --topic <TOPIC-NAME> \
  --reset-offsets --shift-by -100 --dry-run

docker exec kafka /opt/kafka/bin/kafka-consumer-groups.sh \
  --bootstrap-server 192.168.10.250:9094 \
  --command-config /tmp/client.properties \
  --group <GROUP-NAME> \
  --topic <TOPIC-NAME> \
  --reset-offsets --shift-by -100 --execute

## if you want to known partition to rollback

docker exec kafka /opt/kafka/bin/kafka-consumer-groups.sh \
  --bootstrap-server 192.168.10.250:9094 \
  --command-config /tmp/client.properties \
  --group <GROUP-NAME> \
  --topic <TOPIC-NAME>:0 \
  --reset-offsets --shift-by -100 --execute

docker exec kafka /opt/kafka/bin/kafka-consumer-groups.sh \
  --bootstrap-server 192.168.10.250:9094 \
  --command-config /tmp/client.properties \
  --describe --group <GROUP-NAME>

docker exec -it kafka /opt/kafka/bin/kafka-consumer-groups.sh \
  --bootstrap-server 192.168.10.250:9094 \
  --command-config /tmp/client.properties \
  --list

docker exec kafka /opt/kafka/bin/kafka-topics.sh \
  --bootstrap-server 192.168.10.250:9094 \
  --command-config /tmp/client.properties \
  --describe --topic __consumer_offsets

docker exec kafka /opt/kafka/bin/kafka-console-consumer.sh \
  --bootstrap-server 192.168.10.250:9094 \
  --consumer.config /tmp/client.properties \
  --topic __consumer_offsets \
  --formatter "org.apache.kafka.tools.consumer.OffsetsMessageFormatter" \
  --from-beginning \
  --max-messages 20 | jq

docker exec kafka /opt/kafka/bin/kafka-configs.sh \
  --bootstrap-server 192.168.10.250:9094 \
  --command-config /tmp/client.properties \
  --entity-type topics --entity-name fund-transfer --describe


# all config for broker

docker exec kafka /opt/kafka/bin/kafka-configs.sh \
  --bootstrap-server 192.168.10.250:9094 \
  --command-config /tmp/client.properties \
  --entity-type brokers --entity-name 1 --describe \
  --all

# all settings for brokers

docker exec kafka /opt/kafka/bin/kafka-broker-api-versions.sh \
  --bootstrap-server 192.168.10.250:9094 \
  --command-config /tmp/client.properties

docker exec kafka /opt/kafka/bin/kafka-metadata-quorum.sh \
  --bootstrap-server 192.168.10.250:9094 \
  --command-config /tmp/client.properties \
  describe --status


docker exec kafka /opt/kafka/bin/kafka-configs.sh \
  --bootstrap-server 192.168.10.250:9094 \
  --command-config /tmp/client.properties \
  --entity-type brokers --entity-name 1 --describe --all


# if leader get crazy and you want to change leader 

docker exec kafka /opt/kafka/bin/kafka-leader-election.sh \
  --bootstrap-server 192.168.10.250:9094 --command-config /tmp/client.properties \
  --election-type PREFERRED --topic orders --partition 0


docker exec kafka /opt/kafka/bin/kafka-transactions.sh \
  --bootstrap-server 192.168.10.250:9094 --command-config /tmp/client.properties \
  list


docker exec kafka /opt/kafka/bin/kafka-transactions.sh \
  --bootstrap-server 192.168.10.250:9094 --command-config /tmp/client.properties \
  list --duration-filter 60000













